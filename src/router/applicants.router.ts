import { Router } from 'express';
// import { applicantsController } from '../controller';

const router = Router();

// router.get('/', applicantsController.getWithQuery);

export const applicantsRouter = router;
